# Workflow Configuration
# This file contains environment variables used across all GitHub Actions workflows
# Update these values when adapting workflows for a new repository

# Project identification
PACKAGE_NAME=prime-uve
PYPI_PACKAGE_NAME=prime-uve

# Python configuration
PYTHON_VERSION_FILE=.python-version

# Source paths
SOURCE_PATHS=src/
TEST_PATHS=tests/

# Branch configuration
MAIN_BRANCH=main
DEV_BRANCH=dev

# Dependency groups (from pyproject.toml [dependency-groups])
DEV_GROUP=dev

# Feature flags (true/false)
ENABLE_QUARTO=false
ENABLE_LOCALES=false
ENABLE_DOCSTRING_VALIDATION=false

# Documentation patterns (regex for detecting docs-only changes)
DOCS_PATTERNS=^docs/|^examples/|\.md$|^_docs/

# Linting configuration
LINT_COMMAND=uv run ruff check
FORMAT_COMMAND=uv run ruff format
